import React from 'react'

const SectionTitle = ({ title }) => {
    return (
        <div className='text-[65px] font-bold text-transparent bg-clip-text inline-block bg-gradient-to-r from-alfan-white to-alfan-gray mb-8'>{title}</div>
    )
}

export default SectionTitle